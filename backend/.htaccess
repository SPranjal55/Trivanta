Options -Indexes
AddDefaultCharset UTF-8

# Handle CORS preflight
RewriteEngine On
RewriteCond %{REQUEST_METHOD} OPTIONS
RewriteRule ^(.*)$ $1 [R=200,L]

# Note: Do not force Content-Type for all .php files — it breaks setup_database.php (HTML) and test.php diagnostics.
